import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

// Placeholder — the real settings UI arrives in Phase 5.
createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <p style={{ fontFamily: 'system-ui', padding: 24 }}>RELEX settings — coming in Phase 5.</p>
  </StrictMode>,
)
